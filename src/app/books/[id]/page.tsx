import React from 'react';


interface IBookDetailsPageProps {
    params: Promise {
        id: string;
    }
}



const BookDetailsPage = ({params}: IBookDetailsPageProps) => {
const {id} = await


    return (
        <div>
            Book Details Page
        </div>
    );
};

export default page;